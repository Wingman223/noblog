<mvc:View
	controllerName="view.BlogAndPost"
	xmlns="sap.m"
	xmlns:core="sap.ui.core"
	xmlns:mvc="sap.ui.core.mvc" >
	<Page
		title="{i18n>WELCOME_TITLE}"
		enableScrolling="true"
		content="{posts}">
		<footer>
			<Toolbar>
			</Toolbar>
		</footer>
		<content>
			<Panel headerText="{title}">
				<content>
					<Image
						src="{pictureUrl}"
						visible="{=${pictureUrl} ? true : false}"
						width="250px"
					>
					</Image>
					<Text
						text="{content}">
					</Text>
				</content>
			</Panel>
		</content>
	</Page>
</mvc:View>